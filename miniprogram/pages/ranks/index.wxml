<view class="rank-title">{{ name }}</view>
<view>
  <view wx:for="{{ songs }}" wx:key="id" class="music-item-wrap">
    <view class="index">{{ index + 1 }}</view>
    <music-item song="{{ item }}" privilege="{{ privileges[index] }}"></music-item>
  </view>
</view>
